%% Práctica 06: Sistemas diferenciales y en diferencias
%
% *Intituto Politécnico Nacional*
%
% *Unidad Profesional Interdisciplinaria en Ingeniería y Tecnologías Avanzadas*
%
% *Análisis de Señales y Sistemas*
%
% *Grupo: 2MV1*
%
% *Alumnos:*
% 
% * Carmona Bellido Marco Tulio
%
% * Ordorica Muñoz Leonardo Antonio
%
%% Sistemas diferenciales
% Realizar los siguientes problemas para el siguiente ejemplo.
%
% $\frac{dy(t)}{dt}+2\frac{dy(t)}{dt}+2y(t)=\frac{dx(t)}{dt}+2x(t)$
%
% Con $x(t)=e^{-t}u(t)$ y con condiciones iniciales y'(0)=1 y y(0)=1.
%
% Para fines practicos se va a cambiar la definición del escalon en matlab.
%%
sympref('HeavisideAtOrigin', 1);
%% Sistemas diferenciales 1
% Muestra la función de transferencia del sistema.
%
% Para la función de transferencia del sistema necesitamos que la entrada
% del sistema sea un impulso y no hay condiciones iniciales, entonces el
% codigo solo se utilizaria hasta el punto que no aplique la transformada
% inversa.
%
% Para este problema se utilizará una modificación de la función
% "laplace2016a" llamada "transferencia".
%%
syms t;
transferencia([2 2 1],[2 1],[0 0],0,dirac(t),10)
%% Sistemas diferenciales 2
% Muestra la respuesta al impulso (simbólico, gráfica)
%
% Para este problema utilizamos el codigo tal cual, ya que la respuesta al
% impulso es la transformada inversa de la función de transferencia.
%%
syms t;
laplace2016a([2 2 1],[2 1],[0 0],0,dirac(t),10)
%% Sistemas diferenciales 3
% Muestra la respuesta a entrada cero (simbólico, gráfica)
%
% Utilizaremos la misma función pero la entrada será un cero y se utilizan
% las condiciones iniciales dadas.
% Debido a que la entrada es cero se hará una nueva modificación a la
% función y se llamará "entradacero".
%%
syms t;
entradacero([2 2 1],[2 1],[1 1],0,0,10)
%% Sistemas diferenciales 4
% Muestra la respuesta a estado cero (simbólico, gráfica)
%
% Utilizaremos la misma función pero no extistiran condiciones iniciales y
% esta vez utilizaremos la entrada dada.
%%
syms t;
laplace2016a([2 2 1],[2 1],[0 0],0,exp(-t)*heaviside(t),10)
%% Sistemas diferenciales 5
% Muestra la respuesta total (simbólico, gráfica)
%
% Utilizaremos la misma función y esta vez utilizaremos la entrada y las
% condiciones iniciales dadas.
%%
syms t;
laplace2016a([2 2 1],[2 1],[1 1],0,exp(-t)*heaviside(t),10)
%% Sistemas diferenciales 6
% La respuesta total al escalón con condiciones iniciales 0 (simbólico, gráfica)
%
% Ahora utilizaremos como entrada un escalon unitario y condiciones
% iniciales cero.
%%
syms t;
laplace2016a([2 2 1],[2 1],[0 0],0,heaviside(t),10)
%% Sistemas diferenciales 7
% Usando subplot, depliega una figura con 5 gráficas
%
% Para esta parte haremos uso de la función subplot y colocaremos las
% graficas previamente guardadas como figuras.
%%
graficas();
%% Referencias
% # Martínez, R. [Mate y así]. (2019 Marzo). Conceptos de Sistemas
% [Lista de reproducción]. Recuperada de <https://www.youtube.com/playlist?list=PLSC7gO5dTNPd3lHkJ7MJLTodTwo3xV26v https://www.youtube.com/playlist?list=PLSC7gO5dTNPd3lHkJ7MJLTodTwo3xV26v>
% # Lathi, B. P.. (2005). Linear Systems and Signals. New York: Oxford University Press, Inc.
